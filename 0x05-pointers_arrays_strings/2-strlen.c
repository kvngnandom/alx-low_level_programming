#include "main.h"
#include <string.h>
#include <stdio.h>

/**
 * _strlen - checks the length of a string
 * @s:input
 * Return: Always 0 (Success)
*/

int _strlen(char *s)
{
	int length = strlen(s);

	return (length);
}
